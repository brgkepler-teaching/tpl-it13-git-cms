import { defineConfig } from 'astro/config';

// GitHub Pages veröffentlicht unter https://<github-name>.github.io/<repo-name>/
export default defineConfig({
  // 🎯 TODO (Aufgabe 13.2): Entferne die // und trage DEINE Werte ein:
  // site: 'https://<dein-github-name>.github.io',
  // base: '/<dein-repo-name>',
});
